general:
  artifacts:
    - "build/compressed/"

test:
  pre:
    - npm install -g http-server
    - "http-server ./app":
        background: true
  override:
    - grunt build
    - mocha "app/test/*.js"
